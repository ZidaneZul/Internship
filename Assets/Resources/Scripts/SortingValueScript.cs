using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SortingValueScript : MonoBehaviour
{
    /// <summary>
    /// script is required as the editor will search tags and place in array in order but build
    /// will be random instead. This script is used to show what place shoud the item be in 
    /// the array.
    /// </summary>
    public int value;
}
